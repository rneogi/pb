# Public PitchBook Observer - Requirements
# Python 3.12+

# HTTP Client
httpx[http2]>=0.25.0

# HTML/Content Extraction
trafilatura>=1.6.0
beautifulsoup4>=4.12.0
lxml>=4.9.0

# API Framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0

# Configuration
pyyaml>=6.0.0

# Embeddings (local - no API key needed)
sentence-transformers>=2.2.0
torch>=2.0.0

# TF-IDF fallback (used if sentence-transformers unavailable)
scikit-learn>=1.3.0

# Numerical
numpy>=1.24.0

# Visualization
streamlit>=1.32.0
plotly>=5.18.0
pandas>=2.0.0

# Environment
python-dotenv>=1.0.0

# LLM integration (optional - for enhanced answers)
# Only needed if using --llm mode with ANTHROPIC_API_KEY
anthropic>=0.25.0

# Optional: Playwright for JS rendering (company/investor pages)
# pip install playwright && playwright install chromium
# playwright>=1.40.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
